package fen;

/**
* An enum containing all currently supported border modes.
**/
enum BorderMode {
    /**
    * Represents, that no border is used.
    **/
    OFF;

    /**
    * Represents, that the "TALL" border is used.
    **/
    TALL;
}
